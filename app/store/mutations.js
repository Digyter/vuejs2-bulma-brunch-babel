export const CARS = (state, cars) => {
    state.cars = cars;
};

export const PREV_PAGE = (state, prev_page) => {
    state.prev_page = prev_page;
};

export const NEXT_PAGE = (state, next_page) => {
    state.next_page = next_page;
};